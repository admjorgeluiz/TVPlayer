# Arquivos de configuração local específicos do ambiente. NÃO FAÇA COMMIT DESTE ARQUIVO.
local.properties

# Arquivos e pastas de cache e configuração do IDE (Android Studio / IntelliJ)
.idea/
*.iml
*.ipr
*.iws
.DS_Store # Para utilizadores de macOS
captures/ # Capturas de tela do Android Studio
*.log    # Arquivos de log

# Pasta .gradle (cache e histórico do Gradle para este projeto)
# Esta pasta é gerada pelo Gradle e não deve ser versionada.
.gradle/
.kotlin/

# Pastas de Build (saída da compilação)
# Ignora a pasta 'build' na raiz do projeto e em qualquer submódulo (como 'app/build').
**/build/
app/release/
app/debug/

# Arquivos do Gradle Wrapper
# É recomendado versionar o gradle-wrapper.properties para garantir builds consistentes.
# O gradle-wrapper.jar é opcional. Muitos projetos o versionam, outros não.
# Se NÃO quiser versionar o JAR, descomente a linha abaixo:
# gradle/wrapper/gradle-wrapper.jar

# Arquivos de teste de profiler do Android Studio
*.snapshot
*.hprof

# Keystore de Assinatura (NUNCA FAÇA COMMIT DAS SUAS CHAVES PRIVADAS DE ASSINATURA!)
# Se você gerar um arquivo de keystore (ex: .jks, .keystore) para assinar seu app,
# adicione o nome exato do arquivo aqui para evitar commitá-lo acidentalmente.
# Exemplo:
# my-release-key.keystore
# *.jks

# Arquivos específicos do seu projeto que não devem ser versionados:
# gradle-app.setting # Mantenha esta linha se "gradle-app.setting" for um arquivo que você usa
                     # e não quer no controle de versão. Se não souber o que é, pode remover.

