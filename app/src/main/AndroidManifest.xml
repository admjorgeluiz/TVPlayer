<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.tvplayer">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TVPlayer"
        tools:targetApi="31">

        <!-- Tela de Configuração: Tela inicial -->
        <activity
            android:name=".ui.config.ListConfigActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Tela de exibição da lista -->
        <activity
            android:name=".ui.list.ChannelListActivity"
            android:exported="true" />

        <!-- Tela de reprodução -->
        <activity
            android:name=".ui.player.PlayerActivity"
            android:exported="true" />

    </application>

</manifest>
